v1 — behavioural theme clustering
Below are short spending notes a user wrote about their own purchases, with the
category each belongs to. Amounts, dates and identity have been removed.

Group them into at most 4 behavioural themes — the *reason* the spending
happens (tiredness, social pressure, convenience, stress, planning), not the
category. Count how many notes fall in each theme. Count only what you see.

notes:
$notes

For each theme write an insight of one or two sentences that tells the user
something about their own behaviour they would not have spotted — what triggers
the spending and when it happens. Quote the words they actually used where it
helps. Do not state any number other than the count.

Reply with JSON only:
{"themes": [{"theme": "short label", "count": 11, "insight": "one or two sentences"}]}
